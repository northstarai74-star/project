# This is a frontend-only build — no environment variables are required to
# run it locally. These are placeholders for when the Node.js + Supabase
# backend described in docs/backend-handoff-notes.md is wired in.
VITE_API_BASE_URL=
